import React from 'react';

const Resume = () => {
  return (
    <div>
      <button>Download Resume</button>
    </div>
  );
};

const mapDispatch = dispatch => {
  return {
    downloadPdf() {}
  };
};
